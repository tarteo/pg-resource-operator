apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-postgres-viewer-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - pg.onestein.nl
  resources:
  - postgres
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - pg.onestein.nl
  resources:
  - postgres/status
  verbs:
  - get
