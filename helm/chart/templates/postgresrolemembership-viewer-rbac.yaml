apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-postgresrolemembership-viewer-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - pg.onestein.nl
  resources:
  - postgresrolememberships
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - pg.onestein.nl
  resources:
  - postgresrolememberships/status
  verbs:
  - get
